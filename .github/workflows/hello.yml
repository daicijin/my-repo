name: Hello # ワークフロー名
on: push # イベント（この例では、pushイベントがトリガー）
jobs: # ジョブの定義
  hello: # ジョブID
    runs-on: ubuntu-latest # ランナー（この例では、最新のUbuntu環境）
    steps: # ステップの定義
      - name: Hello world action step # ステップ名
        run: echo "Hello world!" # コマンド（この例では、Hello world!を出力）
